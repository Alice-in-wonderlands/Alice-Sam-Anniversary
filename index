<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>1 Year Anniversary</title>
  <style>
    body {
      font-family: 'Arial', sans-serif;
      background-color: #0b0b1d;
      margin: 0;
      padding: 0;
      text-align: center;
      color: #f2eee7;
      overflow-x: hidden;
    }

    /* ============ PASSWORD GATE ============ */
    .password-gate {
      position: fixed;
      top: 0; left: 0;
      width: 100%; height: 100%;
      background-color: #1a0d0d;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      z-index: 10000;
      transition: opacity 1s ease;
    }
    .password-gate h2 {
      color: #f2eee7;
      margin-bottom: 20px;
      font-size: 1.5em;
    }
    .password-gate input {
      padding: 10px 20px;
      border-radius: 20px;
      border: none;
      text-align: center;
      font-size: 1em;
      outline: none;
      background-color: #f2eee7;
      color: #3b2f2f;
      transition: all 0.3s ease;
    }
    .password-gate input.wrong {
      background-color: #ffb3b3;
      color: #590818;
    }
    .password-gate button {
      margin-top: 15px;
      padding: 10px 25px;
      border-radius: 20px;
      border: none;
      cursor: pointer;
      background-color: #590818;
      color: #f2eee7;
      font-size: 1em;
      transition: background-color 0.3s ease;
    }
    .password-gate button:hover {
      background-color: #7a1b27;
    }

    /* ============ OVERLAY INTRO ============ */
    .intro-overlay {
      position: fixed;
      top: 0; left: 0;
      width: 100%; height: 100%;
      background-color: #590818;
      color: #f2eee7;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.5em;
      font-weight: bold;
      z-index: 9999;
      cursor: pointer;
      flex-direction: column;
      overflow: hidden;
    }

    .intro-overlay span {
      font-size: 2em;
      margin-bottom: 20px;
      white-space: pre;
      position: relative;
      z-index: 2;
    }

    .intro-overlay .line2 {
      font-size: 1.5em;
      margin-top: 0;
      z-index: 2;
    }

    .intro-overlay small {
      font-size: 0.6em;
      font-weight: 300;
      opacity: 0;
      margin-top: 25px;
      transition: opacity 1.5s ease;
      z-index: 2;
    }

    .intro-overlay small.visible {
      opacity: 0.8;
    }

    /* Floating icons */
    .floating-icon {
      position: absolute;
      font-size: 1.5em;
      opacity: 0.8;
      animation: floatIcons 10s linear infinite;
    }

    @keyframes floatIcons {
      0% { transform: translateY(100vh) rotate(0deg); opacity: 0; }
      20% { opacity: 1; }
      80% { opacity: 1; }
      100% { transform: translateY(-20vh) rotate(360deg); opacity: 0; }
    }

    /* ============ MAIN PAGE ============ */
    h1 {
      margin-top: 150px;
      color: #f2eee7;
      font-size: 2.2em;
    }

    .film-carousel {
      width: 80%;
      max-width: 1000px;
      height: 260px;
      overflow: hidden;
      margin: 60px auto;
      background-color: #222;
      position: relative;
      border-radius: 10px;
    }

    .film-carousel::before, .film-carousel::after {
      content: '';
      position: absolute;
      left: 0;
      width: 100%;
      height: 20px;
      background-color: #222;
      background-image: radial-gradient(circle, #f2eee7 40%, #222 41%);
      background-size: 40px 40px;
      background-repeat: repeat-x;
      z-index: 2;
    }

    .film-carousel::before { top: 0; }
    .film-carousel::after { bottom: 0; }

    .film-track {
      display: flex;
      min-width: 200%;
      animation: scroll 20s linear infinite;
    }

    .film-track img {
      height: 220px;
      flex-shrink: 0;
      margin: 20px 5px;
      object-fit: cover;
      border-radius: 5px;
    }

    @keyframes scroll {
      0% { transform: translateX(0); }
      100% { transform: translateX(-50%); }
    }

    .tab {
      display: inline-block;
      background-color: #e5e1d8;
      color: #3b2f2f;
      padding: 10px 20px;
      margin: 10px;
      border-radius: 20px;
      cursor: pointer;
      transition: background-color 0.3s;
    }

    .tab:hover { background-color: #d8d4c7; }

    .letter {
      display: none;
      margin: 20px auto 100px auto;
      max-width: 500px;
      background-color: #f7f4ee;
      padding: 20px;
      border-radius: 15px;
      box-shadow: 0 0 10px rgba(0,0,0,0.1);
      text-align: left;
      color: #3b2f2f;
    }

    /* Stars */
    .star {
      position: absolute;
      background: white;
      border-radius: 50%;
      opacity: 0.8;
      animation: twinkle 2s infinite ease-in-out;
    }

    @keyframes twinkle {
      0%, 100% { opacity: 0.4; }
      50% { opacity: 1; }
    }
  </style>
</head>
<body>
  <!-- PASSWORD GATE -->
  <div class="password-gate" id="passwordGate">
    <h2>Enter the password to continue 💌</h2>
    <input type="password" id="pwInput" placeholder="Enter password...">
    <button onclick="checkPassword()">Enter</button>
  </div>

  <!-- INTRO OVERLAY -->
  <div class="intro-overlay" id="introOverlay" onclick="closeIntro()" style="display:none;">
    <span id="line1"></span>
    <span id="line2" class="line2"></span>
    <small id="clickText">(Click anywhere to continue)</small>
  </div>

  <!-- FLOATING ICONS -->
  <script>
    const overlay = document.getElementById('introOverlay');
    const icons = ['💖','✨','🌙','💫','🌸','💌'];
    for(let i=0;i<20;i++){
      const icon = document.createElement('div');
      icon.className = 'floating-icon';
      icon.textContent = icons[Math.floor(Math.random()*icons.length)];
      icon.style.left = Math.random()*100 + 'vw';
      icon.style.animationDuration = (8 + Math.random()*5) + 's';
      icon.style.fontSize = (1 + Math.random()*1.5) + 'em';
      overlay.appendChild(icon);
    }
  </script>

  <!-- AUDIO -->
  <audio id="bg-music" loop>
    <source src="mylove.mp3" type="audio/mpeg">
  </audio>

  <!-- MAIN CONTENT -->
  <h1>Happy 1 Year Anniversary ❤️</h1>
  <div class="film-carousel">
    <div class="film-track">
      <img src="WILL1.jpg" alt="Photo 1">
      <img src="WILL2.jpg" alt="Photo 2">
      <img src="WILL1.jpg" alt="Photo 1">
      <img src="WILL2.jpg" alt="Photo 2">
      <img src="WILL1.jpg" alt="Photo 1">
      <img src="WILL2.jpg" alt="Photo 2">
    </div>
  </div>

  <div>
    <div class="tab" onclick="showLetter('letter1')">Love Letter 1</div>
    <div class="tab" onclick="showLetter('letter2')">Love Letter 2</div>
  </div>

  <div id="letter1" class="letter">
    <h3>Letter 1</h3>
    <p>My love, this year with you has been amazing. Every moment with you is precious...</p>
  </div>

  <div id="letter2" class="letter">
    <h3>Letter 2</h3>
    <p>I still remember the day we met, and I cherish every day spent with you...</p>
  </div>

  <script>
    function checkPassword() {
      const pwInput = document.getElementById('pwInput');
      if (pwInput.value === '1510') {
        const gate = document.getElementById('passwordGate');
        gate.style.opacity = '0';
        setTimeout(() => {
          gate.style.display = 'none';
          document.getElementById('introOverlay').style.display = 'flex';
        }, 800);
      } else {
        pwInput.classList.add('wrong');
        pwInput.value = 'Wrong';
        setTimeout(() => {
          pwInput.classList.remove('wrong');
          pwInput.value = '';
        }, 1000);
      }
    }

    function showLetter(id) {
      const letters = document.querySelectorAll('.letter');
      letters.forEach(letter => letter.style.display = 'none');
      document.getElementById(id).style.display = 'block';
    }

    function closeIntro() {
      document.getElementById('introOverlay').style.display = 'none';
      const music = document.getElementById('bg-music');
      music.volume = 0.7;
      music.play().catch(err => console.log("Autoplay bị chặn:", err));
    }

    // Typing effect
    const text1 = "🎉❤️Happy Anniversary❤️🎉";
    const text2 = "October 2024 - October 2025";
    const line1 = document.getElementById("line1");
    const line2 = document.getElementById("line2");
    const clickText = document.getElementById("clickText");

    function typeText(text, el, callback) {
      let i = 0;
      function typing() {
        if (i < text.length) {
          el.textContent += text.charAt(i);
          i++;
          setTimeout(typing, 100);
        } else if (callback) callback();
      }
      typing();
    }

    window.onload = () => {
      setTimeout(() => {
        typeText(text1, line1, () => {
          typeText(text2, line2, () => {
            clickText.classList.add("visible");
          });
        });
      }, 2000);
    };
  </script>
</body>
</html>
